import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  buttonStyle: {
    color: 'white',
    fontSize: '1.6rem',
    padding: '10px 5px',
    marginTop: '40px',
  },
  inputStyle: {
    fontSize: '1.6rem',
    padding: '5px 5px',
    marginTop: '20px',
  },
});

export default useStyles;
